import { apiHelpers, API_ENDPOINTS } from '../../lib/api-config';

// This file can be used for flight-related API calls using the centralized configuration
// Example usage:
// export async function getFlights() {
//   return await apiHelpers.get(API_ENDPOINTS.FLIGHTS);
// }
